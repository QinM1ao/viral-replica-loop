# Intentionally empty.
#
# The Docker sandbox receives only the host's existing Codex login through a
# read-only mount of ~/.codex/auth.json. Never place client/provider credentials
# (Matpool, Seedance, TOS, MediaKit, Wujie, etc.) in this file.
