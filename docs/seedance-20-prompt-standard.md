# Seedance 2.0 Prompt Standard

The canonical prompt format, validated example, failure case, and completion criteria live in:

```text
.agents/skills/video-replication/references/seedance-20-prompt-standard.md
```

Do not duplicate the format here. The current standard is ordered `time | Shot 01–02` blocks with `画面 / 声音` in every block and an optional `音效：<...>` line only when a useful visible action sound exists.

The preferred provider-validated case is now the 2026-07-17 `job-011` complete 26-second Part1+Part2 result:

```text
.agents/skills/video-replication/references/kongfengchun-final-26s-standard.md
output/job-011/final-delivery/
```

The older fermented-toner example remains only as a source-faithful, no-invented-action reference. `job-002` remains a storyboard-geometry baseline, not the current prompt-writing template.
