# Jobs Bind Immutable Reference Versions

The Reference Library may gain new versions over time, but each Job records an immutable Reference Binding to the exact product, person, video, and audio versions selected at intake. New library versions become defaults only for future Jobs unless a user explicitly rebinds an existing Job; this uses additional storage and version metadata in exchange for reproducible inputs, valid QC evidence, and stable resumed behavior.
