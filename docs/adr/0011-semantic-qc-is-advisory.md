# Semantic QC Is Advisory

Semantic QC cannot block a Usable Artifact from advancing. It may identify one targeted image retry or record a Visual Warning, while only deterministic unusability, wrong-Job or stale binding, missing authorization, or conflicting state can produce a blocking result. This keeps semantic review useful without letting strict or subjective judgment stall a Job Run.
