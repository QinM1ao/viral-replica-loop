# Client Workspace Has Three Information Layers

Each Client Workspace separates reusable customer content, complete per-Job archives, and a plugin-owned system area, while a Delivery Library provides the clean customer-facing exit. This introduces explicit placement rules and a delivery projection, but keeps every intermediate artifact and QC record available without scattering queue state, caches, failed candidates, and active deliverables across one flat root.
