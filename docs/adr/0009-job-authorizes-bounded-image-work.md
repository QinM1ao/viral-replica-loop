# A Job Authorizes Bounded Image Work

Creating a formal Job grants Image Work Authorization for every required storyboard image and one targeted retry for each failed Part, so a Job Run can reach the Pre-Seedance Handoff without another confirmation. The authorization remains bounded by image-run limits and never extends to video generation, MediaKit work, another Job, or further image retries.
