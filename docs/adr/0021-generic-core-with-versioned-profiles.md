# Generic Core with Versioned Profiles

The Viral Replica Plugin provides one generic replication core, may ship reusable validated behavior as Built-in Profiles such as the existing Kongfengchun specialization, and keeps private client specialization in Workspace Profiles. This requires explicit profile routing and extension boundaries, but preserves validated behavior without hard-coding one customer into the core or leaking one client's rules into another Client Workspace.
