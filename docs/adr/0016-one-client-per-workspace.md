# One Client per Workspace

Each Client Workspace belongs to exactly one client or brand organization and may contain multiple products, people, reference videos, and Jobs. Separate clients never share one workspace; this duplicates a small amount of workspace metadata but prevents cross-client asset contamination, keeps privacy and retention boundaries clear, and still allows references to be reused across that client's Jobs.
