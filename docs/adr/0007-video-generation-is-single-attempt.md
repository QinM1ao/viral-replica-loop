# Video Generation Is Single-Attempt

A Job Run never automatically resubmits video generation. Any playable output with the required streams, materially correct duration, and successful decode continues through finishing and delivery; visual or creative defects are recorded as Visual Warnings, while missing, unreadable, or unusable media stops because no downstream artifact exists. A later video retake is a separate explicitly requested and approved Job Run.
