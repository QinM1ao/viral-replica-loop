# First Use Includes Automatic Workspace Bootstrap

“Ready to use” means a user may complete one plugin-led Workspace Bootstrap that creates the Client Workspace, prepares private runtime dependencies, verifies required external capabilities, and reports any authorization still needed without requiring terminal commands or code edits. This adds installer and diagnostic responsibilities to the plugin, but keeps the distributed product honest about its dependencies while making every later Job begin from the same validated environment.
